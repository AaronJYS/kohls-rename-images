name: Test and deploy web app

on:
  push:
    branches: [main, beta]
  pull_request:
    branches: [main, beta]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: Test on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    env:
      PYTHON: python
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Check JavaScript syntax
        run: |
          node --check web/app.js
          node --check web/renamer.js
          node --check web/folder-io.js
          node --check web/aafes-parser.js
          node --check web/pdf-reader.js
          node --check web/excel-export.js
          node --check web/pdf-app.js
          node --check web/tool-nav.js
      - name: Verify image conversion, PDF extraction, and Excel output
        run: npm test

  deploy:
    name: Deploy to GitHub Pages
    needs: test
    if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/beta') && github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    concurrency:
      group: github-pages
      cancel-in-progress: false
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Check out production
        uses: actions/checkout@v7
        with:
          ref: main
          path: production
      - name: Check out beta
        uses: actions/checkout@v7
        with:
          ref: beta
          path: beta
      - name: Assemble production and beta sites
        run: |
          mkdir -p site/beta
          cp -a production/web/. site/
          cp -a beta/web/. site/beta/
      - uses: actions/configure-pages@v6
      - uses: actions/upload-pages-artifact@v5
        with:
          path: site
      - name: Publish web app
        id: deployment
        uses: actions/deploy-pages@v5
